using UnityEngine;
using System.Collections;
using System.Collections.Generic;

[ExecuteInEditMode]
public class UIMessageAlertViewController : UIAlertViewControllerBase 
{
	/*
	public MYLabel messageLabel = null;

	protected override void DoAwake()
	{
		base.DoAwake();
	}
	
	protected override void DoUpdate () 
	{
		base.DoUpdate();
	}
	
	public void Show(string title_, string message_, string confirmButtonTitle_, string[] otherButtonTitles_, bool showTitleIcon_)
	{
		if (Application.isEditor && !Application.isPlaying)
		{
			Debug.LogWarning("Show can only be called in play mode");
			return;
		}

		InitializeLabelWithTextSafely(messageLabel, message_);
		base.Show( title_, confirmButtonTitle_, otherButtonTitles_, showTitleIcon_);
	}

	protected override void Reposition()
	{
		base.Reposition();
	}
	*/
}
